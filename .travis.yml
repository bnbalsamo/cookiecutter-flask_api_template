language: python
python:
    - "3.5"
before_script:
    - pip install cookiecutter
script: cookiecutter . --no-input && cd MyProject && python setup.py install && python tests.py && docker build . -t proj && docker run proj python tests.py 
services:
    - docker
